new Effect.Fade('lev1', { duration: 0.3 });
new Effect.Fade('trans1', { duration: 0.3 });
var element = $('<%= @element_id %>');
if(element)
  element.update('<%= escape_javascript(eval(@helper)) %>')
